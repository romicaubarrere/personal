---
interface Props {
  current: 'es' | 'en' | 'pt';
  page?: 'home' | 'work' | 'community' | 'education' | 'questions' | 'when-you-can';
}

const { current, page = 'home' } = Astro.props;
const paths = {
  home: { es: '/personal/', en: '/personal/en.html', pt: '/personal/pt.html' },
  work: { es: '/personal/como-trabajo.html', en: '/personal/en/como-trabajo.html', pt: '/personal/pt/como-trabajo.html' },
  community: { es: '/personal/comunidad-charlas.html', en: '/personal/en/comunidad-charlas.html', pt: '/personal/pt/comunidad-charlas.html' },
  education: { es: '/personal/formacion.html', en: '/personal/en/formacion.html', pt: '/personal/pt/formacion.html' },
  questions: { es: '/personal/posts/por-que-hago-tantas-preguntas.html', en: '/personal/en/posts/why-do-i-ask-so-many-questions.html', pt: '/personal/pt/posts/por-que-faco-tantas-perguntas.html' },
  'when-you-can': { es: '/personal/posts/cuando-puedas.html', en: '/personal/en/posts/when-you-can.html', pt: '/personal/pt/posts/quando-puder.html' }
} as const;
const links = [
  { code: 'es', label: 'Español', href: paths[page].es },
  { code: 'en', label: 'English', href: paths[page].en },
  { code: 'pt', label: 'Português', href: paths[page].pt }
] as const;
---

<nav class="language-switcher" aria-label={current === 'es' ? 'Elegir idioma' : current === 'en' ? 'Choose language' : 'Escolher idioma'}>
  {links.map((link) => (
    <a href={link.href} lang={link.code} hreflang={link.code} aria-current={link.code === current ? 'page' : undefined}>
      <span aria-hidden="true">{link.code.toUpperCase()}</span>
      <span class="sr-only">{link.label}</span>
    </a>
  ))}
</nav>

<style>
  .language-switcher{position:absolute;z-index:130;top:14px;right:18px;display:flex;gap:6px;padding:6px;background:#faf3e4;border:2px solid #241f18;box-shadow:4px 4px 0 rgba(22,19,16,.28);transform:rotate(.4deg)}
  .language-switcher a{display:grid;place-items:center;min-width:44px;min-height:44px;padding:0 8px;color:#241f18;font:700 13px/1 "DM Sans",sans-serif;text-decoration:none;border:2px solid transparent}
  .language-switcher a:hover,.language-switcher a:focus-visible{border-color:#8f4230;outline:none}
  .language-switcher a[aria-current="page"]{color:#faf3e4;background:#22432c;border-color:#22432c}
  @media(max-width:760px){
    .language-switcher{position:fixed;top:8px;right:auto;bottom:auto;left:10px;width:max-content;max-width:calc(100vw - 118px);margin:0;display:flex;flex-direction:row;gap:4px;padding:4px;overflow:visible;transform:none}
    .language-switcher a{min-width:40px;min-height:44px;padding:0 6px}
  }
</style>
